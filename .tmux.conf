# Remap prefix from 'C-b' to 'C-a'
#unbind C-b
#set-option -g prefix C-a
#bind-key C-a send-prefix

# Split panes using | and _
bind | split-windows -h
bind _ split-windows -v
#unbind '"'
#unbind '%'

# Reaload config file
bind r source-file ~/.tmux.conf
